# 교사용 진행 안내

수업 목표·교수학습 과정·학생과의 상호작용·평가·참고 영상은 [별도 수업계획서](LESSON_PLAN.md)를 참고하세요.

대상: 고등학교 2학년. 목표: 구체적으로 요청하고, 앱의 동작을 검증하며, 근거를 들어 개선하기. 설치 속도·수정 횟수·화려한 디자인을 성취 기준으로 삼지 않습니다.

## 사전 준비

- RTX 2060 6GB 교육장 PC에서 점검 배치를 실행합니다. 드라이버·설치 상태의 사전 기준과 실제 추론 성공은 별개입니다.
- Ollama 설치 파일, VS Code·Twinny 설치 수단, 모델 두 개를 준비합니다. 학교 보안 정책과 오프라인 확장 의존성도 확인합니다.
- `qwen2.5-coder:3b`는 채팅, `qwen2.5-coder:1.5b-base`는 FIM입니다. 기본 모델을 채팅에 지정하지 않습니다.
- 모델 배포 시 Ollama를 종료하고 `blobs`와 `manifests`를 함께 복사합니다. `OLLAMA_MODELS`가 설정된 PC는 해당 경로를 사용합니다.
- 웹 자료는 전체 폴더로 배포합니다. 학생이 직접 복사·저장·실행할 수 있는지 확인합니다.
- 선택 환경변수 `OLLAMA_FLASH_ATTENTION=1`, `OLLAMA_KV_CACHE_TYPE=q8_0`는 사전 적용합니다. 다운로드 완료 후 트레이 Quit → 재실행. 현재 cmd의 변수 출력만으로 서버 적용을 확정하지 않습니다.
- 교사용 설정 화면 캡처를 확보합니다. 실제 교육장 화면이 없는 상태에서 임의 스크린샷을 검증 자료로 사용하지 않습니다.

## 확인된 범위와 수업 전 기록

교사 보고: 두 모델 조합의 VS Code 연결·실행 성공. 1.5B 채팅에서 반복 응답 경험. 정확한 당시 태그는 미확인입니다. 아래 항목은 아직 실측 기록이 없으므로 수업 전 채웁니다.

| 항목 | 기록 |
|---|---|
| 점검 날짜·PC 사양·Windows·NVIDIA 드라이버 | 미기록 |
| Ollama 버전 (`ollama --version`) | 미기록 |
| VS Code 버전 (`code --version`) | 미기록 |
| Twinny 버전·확장 ID | 미기록 |
| 두 모델 태그·ID (`ollama list`) | 미기록 |
| Chat Provider / Host / Port / Path | 기존 안내: ollama / localhost / 11434 / /v1 — 배포 버전에서 재확인 |
| FIM Provider / Host / Port / Path | 기존 안내: ollama / localhost / 11434 / /api/generate — 배포 버전에서 재확인 |
| 정확한 Qwen 호환 FIM 템플릿 이름·종료 조건·출력 길이 | 미기록 |
| 실제 컨텍스트 설정·`ollama ps` 결과 | 미기록 |
| 첫 실행 / 이후 생성 시간 / 수정 성공 여부 | 미기록 |

FIM 템플릿이 확인되지 않으면 다른 계열을 임의 선택하지 않고 선택 실습을 생략합니다. 화면과 문서가 다르면 배포할 버전에서 동작을 확인한 뒤 학생 안내를 맞춥니다.

## 수업 전 리허설

1. 터미널 3B 채팅에 짧은 코드 질문을 보내고, 실행 중 `ollama ps`를 확인합니다.
2. 같은 질문을 Twinny Chat에서 확인합니다.
3. 본문의 가위바위보 생성 → 점수판 추가 → 초기화 테스트를 3회 진행합니다. 초기 생성과 수정 중 코드 잘림·반복 응답·외부 의존성을 기록합니다.
4. FIM은 다음 코드의 빈 줄에 커서를 놓고 중간 코드만 제안하는지 봅니다.

```js
function add(a, b) {
  // 이 주석을 지우고 이 위치에 커서를 둡니다.
}
console.log(add(2, 3));
```

예상 제안은 `return a + b;`처럼 함수 본문에 들어가는 코드입니다. 설명문·코드 울타리·앞뒤 코드 중복이 없어야 합니다. 반환값 5도 확인합니다.

5. FIM 켬/끔 상태에서 채팅 생성 시간을 비교합니다. 두 모델 동시 사용이 느리거나 GPU/CPU 분할로 지연되면 FIM을 끄고, 필요시 `ollama stop qwen2.5-coder:1.5b-base`로 내려둡니다.
6. 활동지 작성·텍스트 다운로드·인쇄 미리보기, 예제 오프라인 실행을 확인합니다.

## 100분 시간표와 복귀 경로

| 시간 | 활동 | 운영 메모 |
|---|---|---|
| 0–8 | 예제 시연·목표 | 완성 기준 3가지 소개 |
| 8–20 | 준비·실행 | 다운로드는 사전 배포가 기본 |
| 20–32 | 03·04·07 핵심, 08 연습 1·5 | 토큰 예측·로컬 구조·답변 검증, 상세 이론 생략 |
| 32–42 | Twinny Chat 연결 | FIM은 이미 설정된 PC에서만 짧게 체험 |
| 42–62 | 가위바위보·점수판 | 꾸미기는 선택 |
| 62–82 | 개인 앱 | 기능 두 개로 범위 제한 |
| 82–92 | 짝 테스트·개선 | 정상·빈 입력·반복 조작, 같은 절차 재검증 |
| 92–100 | 공유·활동지 | HTML + 활동지 제출 |

20분까지 모델 응답이 없으면 조교가 별도로 확인하고 학생은 짝 실습에 합류합니다. 두 학생은 요청 작성과 테스트 역할을 번갈아 맡습니다. 코드 생성에 막히면 `code/game_starter.html`을 내려받아 문구·기능 수정부터 시작합니다. 지연이 생기면 꾸미기·FIM을 생략하고 테스트와 마무리 시간은 확보합니다.

## 반복 응답 대응

생성 중지 → 정확한 Chat 모델 이름 확인 → 새 대화에 현재 코드·한 가지 요청 전달 → 터미널과 확장 비교 순서입니다. 인사말이 짧아서 발생했다거나 KV 캐시가 찼다고 단정하지 않습니다. 1.5B 채팅 경험은 1.5B-base의 FIM 품질과 구분합니다. 반복 억제 값을 무작정 높이지 않습니다.

## 평가·다음 차시

- 구조 설명: VS Code는 편집·요청, Ollama는 로컬 서버, 모델은 응답 생성 역할을 설명한다.
- 요청: 기능·제약·완료 기준을 적는다.
- 검증: 예상과 실제 결과를 테스트 3개로 비교한다.
- 개선: 문제 또는 개선 필요의 근거를 제시하고 수정 후 재확인한다.

14–16 파이썬·에러 튜터는 다음 차시입니다. 05–06 모델·KV 캐시 상세 자료는 질문이 생겼을 때 참고합니다.

## 캐릭터

`assets/llama-expressions.png`는 AI로 생성한 5열 × 2행 치비 라마 시트입니다. 인사·설명·생각·발견·응원 / 조사·당황·반복 루프·코딩·축하의 10표정으로, 본문 CSS에서 해당 칸을 표시합니다. 글자는 이미지와 분리하며 기존 이미지 파일은 보존했습니다.

## 자료 자체의 검증 기록 · 2026-09-07

로컬 Chromium에서 17개 챕터 이동, 1280px/390px 화면(선택 자료 펼침 포함)의 가로 넘침 없음, 자료 링크, 오프라인 본문·게임 실행을 확인했습니다. 예제의 승패 9가지 조합·점수·초기화, 활동지 임시 저장·복원·텍스트 다운로드·PDF 출력·삭제도 확인했습니다. JavaScript 실행 오류는 없었습니다. 배치 파일은 CP949/CRLF와 본문 소스 일치를 확인했습니다.

이 검증은 Windows 배치 실행이나 RTX 2060에서의 모델 추론·Twinny FIM 품질 검증을 포함하지 않습니다. 해당 항목은 위 리허설에서 확인합니다.


## 다음 차시 운영 변경 · 2026-09-08

14–16장은 **기존 Qwen + VS Code로 PySide6 GUI를 바이브코딩**하는 실습입니다. venv 생성·패키지 설치는 수업 전에 준비하고, 새 cmd 터미널에서 재활성화하도록 안내합니다. Python·PySide6 호환성과 학교 PC에서의 실제 GUI 실행을 확인하세요. 기존 tkinter 코드는 PySide6 참고본으로 교체했습니다.

- 학생은 먼저 API 없는 화면을 생성하고 창 크기·입출력·버튼 상태를 확인합니다.
- 이후 requests 호출을 QThread.run으로 옮기고 결과를 Signal → 메인 창 Slot으로 전달하게 요청합니다.
- 분석 중 지우기·입력 편집·중복 요청을 막고, 창 닫기는 안내 후 보류합니다. 작업 중 QThread 파괴나 강제 terminate로 해결하지 않습니다.
- Ollama 연결 실패 시험은 수정 코드를 먼저 받은 뒤 서버를 종료해서 합니다. 서버가 꺼지면 Twinny 채팅도 멈추므로 시험 후 다시 켭니다.
- 참고본은 구조 비교·복귀용이며 학생 생성물이 동일한 코드일 필요는 없습니다.
- 설치 후 `python -m pip freeze`로 검증된 PC의 정확한 패키지 버전을 별도 기록합니다. requirements.txt는 허용 범위이며 버전 고정 파일이 아닙니다.
- 오프라인 설치는 같은 Windows/Python/아키텍처의 준비 PC에서 `python -m pip download -r requirements.txt -d wheels`로 의존성을 함께 확보하고, 학생 venv에서 `python -m pip install --no-index --find-links wheels -r requirements.txt`로 설치합니다. venv 폴더 자체는 배포하지 않습니다.

### PySide6 참고본 검증 · 2026-09-08

임시 venv의 Python 3.13 / PySide6 6.11.2 / requests 2.34.2에서 Qt offscreen 테스트를 실행했습니다. 빈 입력, 느린 작업 중 이벤트 루프 반응·중복 방지·닫기 보류, 오류별 안내·재시도, HTTP 요청 형식과 잘못된 응답 처리를 4개 테스트로 확인했습니다. 실행 명령은 venv에서 `python -m unittest discover -s tests -v`입니다. 고정 답변으로 GUI 화면도 확인했습니다.

이 검증은 모의 HTTP 응답을 사용했으며 Windows 교육장 PC의 실제 Qwen 응답 시간·품질은 별도 리허설 대상입니다. 생성 프롬프트가 매번 같은 코드를 만든다는 보장은 없으므로 학생 생성물도 본문의 체크리스트로 검증합니다.
